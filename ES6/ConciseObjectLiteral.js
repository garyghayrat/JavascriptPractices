const createPerson = (name, age, gender) => {
    // Only change code below this line
return {
    name, age, gender 
    }
    // Only change code above this line
  };

  console.log(createPerson("john", 19, "male"));

  // or with (), it would looks similar to
  // const getMousePosition = (x, y) => ({x, y});